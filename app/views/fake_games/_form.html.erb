<%= form_with(model: fake_game) do |form| %>
  <% if fake_game.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fake_game.errors.count, "error") %> prohibited this fake_game from being saved:</h2>

      <ul>
        <% fake_game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :genre, style: "display: block" %>
    <%= form.text_field :genre %>
  </div>

  <div>
    <%= form.label :platform, style: "display: block" %>
    <%= form.text_field :platform %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
